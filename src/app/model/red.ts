export class Red {
  id: number;
  nombre: string;
  icon: string;
  link: string;

  constructor(nombre: string, icon: string, link: string) {
    this.nombre = nombre;
    this.icon = icon;
    this.link = link;
  }
}
